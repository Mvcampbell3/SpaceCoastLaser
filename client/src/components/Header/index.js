import React from 'react';
import { Link } from "react-router-dom"

import "./header.css"

const Header = (props) => {
  return (
    <header>
      Space Coast Laser
    </header>
  );
}

export default Header;